import os

class Course:
    # image = os.path.join(app.)
    def __init__(self, name, description, max_participants, image_path):
        self.name = name
        self.description = description
        self.max_participants = max_participants

def load_courses(app):
    
    pass